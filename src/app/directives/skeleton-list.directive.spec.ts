import { SkeletonListDirective } from './skeleton-list.directive';

describe('SkeletonListDirective', () => {
  it('should create an instance', () => {
    const directive = new SkeletonListDirective();
    expect(directive).toBeTruthy();
  });
});
