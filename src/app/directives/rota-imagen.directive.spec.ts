import { RotaImagenDirective } from './rota-imagen.directive';

describe('RotaImagenDirective', () => {
  it('should create an instance', () => {
    const directive = new RotaImagenDirective();
    expect(directive).toBeTruthy();
  });
});
