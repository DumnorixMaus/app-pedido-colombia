import { OnlyNumeroDirective } from './only-numero.directive';

describe('OnlyNumeroDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyNumeroDirective();
    expect(directive).toBeTruthy();
  });
});
