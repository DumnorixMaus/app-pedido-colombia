import { TipoPedidoPipe } from './tipo-pedido.pipe';

describe('TipoPedidoPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoPedidoPipe();
    expect(pipe).toBeTruthy();
  });
});
