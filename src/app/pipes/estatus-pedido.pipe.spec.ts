import { EstatusPedidoPipe } from './estatus-pedido.pipe';

describe('EstatusPedidoPipe', () => {
  it('create an instance', () => {
    const pipe = new EstatusPedidoPipe();
    expect(pipe).toBeTruthy();
  });
});
